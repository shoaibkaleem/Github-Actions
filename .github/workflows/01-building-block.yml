name: 01 - building blocks

on: push

jobs:
  echo-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo "hello world!"
  echo-goodbye:
    runs-on: ubuntu-latest
    steps:
      - name: Failed steps
        run: |
          echo "i will fail"
          exit 1
      - name: Say goodbye
        run: echo "Goodbye!"